name: CoderCo custom action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: hello world custom action
        uses: JamesLBrocklehurst/CoderCo-CustomAction@main
        with:
          who-to-greet: 'CoderCo Team'